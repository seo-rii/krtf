# 표면형 합성 감사 — core_link / full_surface (M2)

`한전노조`의 `한전`은 한국전력공사를 가리키지만 `한전노조` 전체는 다른 조직이고, `금감원장`은 사람이다. VARIANTS_PLAN §2 불변조건 ②는 부모 entity가 전체 표면형을 가져가는 것을 금지한다. 이 리포트는 그 현상이 실제 텍스트에서 **얼마나 자주** 일어나는지를 센다.

재현: `python -m eval.run_composition_audit` (대조군은 M2 이전 체크아웃에서 같은 스크립트를 돌려 `--compare`로 넘긴다 — 두 arm은 같은 seed·같은 표본이다).

표본: KLUE 등 실문장 **10,000문장**, realorg glossary.

## 무엇이 얼마나 바뀌었나

| 지표 | 대조군(M1) | M2 |
|---|---:|---:|
| mention 수 | 3,867 | 3,867 |
| RESOLVED 확정 | 770 | 771 |
| core보다 넓은 표면형 | 0 | 479 |
| commit 보류된 후보 슬롯 | 27 | 763 |

대조군의 `core보다 넓은 표면형`이 0인 것은 그 현상이 없어서가 아니라 **그 빌드가 구분을 표현할 수 없어서**다. 세는 것은 M2가 처음이다.

## 전체 표면형이 core와 같은 것을 가리키는가

| 판정 | 건수 |
|---|---:|
| `DISTINCT_FROM_CORE` | 356 |
| `UNKNOWN` | 102 |
| `SAME_AS_CORE` | 21 |

## core → 전체 표면형 관계

| 관계 | 건수 |
|---|---:|
| `NAMED_VARIANT` | 210 |
| `UNKNOWN` | 102 |
| `ROLE_OF` | 81 |
| `AFFILIATE_OF` | 51 |
| `IDENTITY` | 21 |
| `PART_OF` | 13 |
| `DERIVED_FROM` | 1 |

## commit 보류 사유

prediction set **멤버** 단위 집계다 — mention 수가 아니고, 한 mention이 여러 후보를 담으면 여러 번 세어진다. 확정(RESOLVED) 수와는 위 표에서 따로 본다.

| 사유 | 건수 |
|---|---:|
| `typed_derivative` | 736 |
| `unknown_residual_derivative` | 27 |

## 실제 사례

- **AFFILIATE_OF**: `우리카드 (core 우리)`, `보건대학 (core 보건)`, `현대중공업그룹 (core 현대중공업)`, `신한카드 (core 신한)`, `롯데카드 (core 롯데)`, `롯데건설 (core 롯데)`
- **DERIVED_FROM**: `KBS노조 (core KBS)`
- **IDENTITY**: `국무원 (core 국무)`, `대의원 (core 대의)`, `전 KBS (core KBS)`, `기도처 (core 기도)`, `해경국 (core 해경)`, `문화재위원회 (core 문화재)`
- **NAMED_VARIANT**: `대한민국 (core 대한)`, `참여정부 (core 참여)`, `생명기원 (core 생명)`, `보건과학대학 (core 보건)`, `감사위원 (core 감사)`, `금융시장 (core 금융)`
- **PART_OF**: `금융연구원 (core 금융)`, `제주지사 (core 제주)`, `전라북도지사 (core 전라북도)`, `제주도지사 (core 제주도)`, `MBC본부 (core MBC)`, `KBS본부 (core KBS)`
- **ROLE_OF**: `기재차관 (core 기재)`, `산업부장관 (core 산업부)`, `국가대표 (core 국가)`, `대구시장 (core 대구시)`, `미래부장관 (core 미래부)`, `농협은행장 (core 농협은행)`
- **UNKNOWN**: `카카오톡 (core 카카오)`, `KT한국광고학회 (core KT)`, `한국경제 (core 한국경)`, `국민은행티맥스 (core 국민은행)`, `대한항공서 (core 대한항공)`, `이른바 소프트캡 (core 소프트)`

## 읽는 법과 한계

- `NAMED_VARIANT`가 많은 것은 카탈로그 suffix 앞에 수식어가 붙은 경우(`서울본부`)다. head가 `NAME_PART`여도 앞에 수식어가 있으면 전체는 다른 이름이므로 DISTINCT로 간다.
- 이 표본의 mention에는 Level B 채널이 만든 **잘못된 분해**도 섞여 있다(예: `금융시장`을 core + `장`으로 읽는 경우). 그런 후보는 commit이 보류되므로 precision에는 영향이 없지만, 응답에 관계 필드가 붙는다는 뜻이다 — 숨기지 않고 여기 노출한다.
- **카탈로그 자체의 한계가 예시에 보인다**: `제주도지사`가 `PART_OF`로 분류되는데, 여기 `지사`는 支社(지점)가 아니라 知事(도지사, 사람)다. suffix 하나가 두 의미를 갖는 경우는 표면형만으로 가를 수 없고, 카탈로그 확장(M3)에서 다룬다. 두 해석 모두 DISTINCT라 commit 안전성에는 차이가 없고, 틀리는 것은 relation 라벨이다.
- `named_by_registered_relation`이 0건인 것은 realorg glossary에 `COMPOSES_TO` 선언이 없기 때문이다. 이 수는 **미등록 파생의 규모 신호**이며 M4 variant mining의 우선순위 입력이다.
- 이 리포트는 빈도를 세며, 각 판정이 옳은지는 채점하지 않는다. 정오 판정에는 human-gold seed(M0 잔여 항목)가 필요하다.

*측정 시점: commit `f121ecf`, 2026-08-31 · 표본 10,000문장. 리포트와 코드가 어긋나면 코드가 맞다 — 재생성해서 확인할 것.*

*generated by `python -m eval.run_composition_audit`*