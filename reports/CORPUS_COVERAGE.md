# 코퍼스를 하나 더 넣으면 무엇이 늘어나는가

등록 entity 170개 중 silver 표면형이 모두 `DETECTION_ONLY`로 보류된 4개를 빼면 측정 대상은 **166개**다. 캐시된 코퍼스 6개가 그중 **166개**에 도달한다 (Wilson 95% [0.977, 1.000]).

| 코퍼스 | 문장 | silver 발생 | entity | **단독 도달** |
|---|---:|---:|---:|---:|
| `holdout` | 29,735 | 3,939 | 141 | **0** |
| `holdout2` | 18,000 | 3,156 | 138 | **0** |
| `holdout3` | 46,000 | 7,988 | 141 | **0** |
| `law` | 21,061 | 1,710 | 82 | **0** |
| `web` | 34,000 | 890 | 102 | **2** |
| `wild` | 114,605 | 6,916 | 160 | **3** |

**단독 도달** = 그 코퍼스에만 나오는 entity 수. 이 열이 왜 중요한지: `holdout3`·`web`·`law`를 연달아 넣었고 셋 다 단독 도달이 0이었다. 문장 수는 늘었지만 사전 커버리지는 늘지 않았다.

## 도달하지 못한 entity

없음 — 측정 대상 entity는 모두 최소 1회 등장한다.

## 희박한 entity (5회 이하)

여기가 실제 공백이다. 도달은 했으나 신뢰구간이 무의미할 만큼 적게 나온다. 일반 뉴스·웹을 더 넣어도 이 목록은 거의 움직이지 않는다 — 필요한 것은 해당 도메인의 텍스트다.

- 1회 · `ORG_GOE` 경기도교육청
- 1회 · `ORG_KICE` 한국교육과정평가원
- 1회 · `ORG_KTNG` KT&G
- 1회 · `ORG_NKAS` 국민안전처
- 3회 · `ORG_NFS` 국립과학수사연구원
- 4회 · `ORG_JCS` 합동참모본부
- 4회 · `ORG_KBA` 대한변호사협회

## 보류된 entity (측정 대상 아님)

silver 표면형이 전부 `DETECTION_ONLY`인 경우다. 짧거나 일반어와 겹쳐 은silver 라벨로 쓰지 않기로 한 것이므로, 코퍼스를 아무리 늘려도 이 census에는 나타나지 않는다. 공백으로 읽으면 안 된다.

- `ORG_BLUEHOUSE` 청와대
- `ORG_KAKAO` 카카오
- `ORG_KT` KT
- `ORG_NAVER` 네이버

*측정 시점: commit `bd94f9e-dirty`, 2026-09-08 · 코퍼스 `50c47aec4e66acf1` (114,605문장, 11개 출처). **작업 트리가 커밋과 다르다 — 이 수치는 어떤 커밋에도 없는 코드의 것이다.** 리포트와 코드가 어긋나면 코드가 맞다 — 재생성해서 확인할 것.*

*generated by `python -m eval.run_corpus_coverage`*